version: "2"
services:
  REST:
    image: econnectacs.azurecr.io/explorer/explorer:1.1.6
    container_name: blk-explorer-free-v2
    ports:
      - 8081:8081
    environment:
      - JAVA_OPTS=
      - EXPLORER_PORT=8081
      - NODE_ENDPOINT=${NODE_ENDPOINT}
      - MONGO_CLIENT_URI=mongodb://docker.for.mac.host.internal:28017
      - MONGO_DB_NAME=test
      - UI_IP=http://localhost:5000
    depends_on:
      - mongodb

  mongodb:
    image: mongo:3.4.10
    container_name: blk-mongodb-free
    ports:
      - 28017:27017
    entrypoint: mongod --smallfiles --logpath=/dev/null --bind_ip "0.0.0.0"

  web:
    image: econnectacs.azurecr.io/explorer/explorer-ui:1.6.6
    container_name: blk-explorer-ui-free
    ports:
      - 5000:5000
    environment:
      - REACT_APP_EXPLORER=http://localhost:8081
